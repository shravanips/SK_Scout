# Copy this file to .env and fill in your values.
# Then load it with: export $(cat .env | xargs)
# Or use python-dotenv: pip install python-dotenv

# GitHub Personal Access Token
# Creates at: https://github.com/settings/tokens
# Scopes needed: none (public data only) — but a token raises rate limit
# from 60 req/hr to 5 000 req/hr
GITHUB_TOKEN=ghp_your_token_here
