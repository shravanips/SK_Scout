Working file to check if my github is connected 

Creating a branch

git checkout -b shravani

Push it:
git push -u origin shravani

Daily workflow:
When you start working:
git pull origin main

After making changes:
git add .
git commit -m "Added scraping module"
git push

Merging into main:
1. Push your branch
2. Go to GitHub
3. Create Pull Request (PR)
4. Review → Merge


First get latest changes
git checkout main
git pull origin main

Switch to your branch
git checkout shravani
git merge main

After editing files
git add .
git commit -m "Added new scraping files"
git push



""""""
TOPIC_BUCKETS = {
    "hollywood": [
        "hollywood", "movie", "film", "actor", "actress", "celebrity",
        "oscar", "netflix", "disney", "warner bros", "paramount"
    ],
    "cybersecurity": [
        "cyberattack", "breach", "hack", "leak", "phishing",
        "ransomware", "malware", "deepfake", "scam"
    ],
    "politics": [
        "election", "president", "government", "senate", "policy"
    ],
    "technology": [
        "AI", "OpenAI", "Google", "Microsoft", "startup", "chip", "robot"
    ],
    "finance": [
        "stock market", "inflation", "recession", "fed", "bitcoin"
    ],
    "breaking_news": [
        "breaking news", "shooting", "earthquake", "wildfire", "flood"
    ]
}
"""

04/17

git checkout main
git fetch origin
git pull origin main

Then go back to your branch and bring those updates in:
git checkout shravani
git merge main


SK_SCOUT/Shravani/
├── ingest.py          ← download + parse + all new signal extraction
├── analytics.py       ← all analysis classes + report generation  
├── run_pipeline.py    ← one command to run everything
├── requirements.txt
└── notebooks/
    └── exploration.ipynb  ← interactive version, cell by cell
    